<h2><%= @task.title %></h2>

<p><%= @task.details %></p>
<p>Completed : <%= @task.completed ? "Yes" : "No" %></p>
<p><%= link_to "Update", edit_task_path(@task) %></p>
<p><%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure?" }  %></p>

<%= link_to "Return to the TO DO list", tasks_path %>
